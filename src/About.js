import React from 'react'
import HomeSection from './components/HomeSection'

const About = () => {
  const data = {
    name: "Our E-Pooja Booking Website",
  };

  return <HomeSection myData={data} />;
};

export default About